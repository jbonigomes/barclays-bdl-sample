import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import Button from '../../components/Button'

<Meta title="Button" />

# Preview

Secondary disabled small buttons have a min width.

<Preview>
  <Story name="15 - Secondary disabled small button min width">
    <Button
      small
      disabled
      text="Yes"
      type="secondary"
      onClick={action('clicked')}
    />
  </Story>
</Preview>

# Props

<Props of={Button} />
