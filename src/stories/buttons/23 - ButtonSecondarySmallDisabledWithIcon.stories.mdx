import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import Button from '../../components/Button'

<Meta title="Button" />

# Preview

Secondary disabled small button with icon

<Preview>
  <Story name="23 - Secondary disabled small button with icon">
    <Button
      small
      disabled
      icon="card"
      type="secondary"
      text="Button with icon"
      onClick={action('clicked')}
    />
  </Story>
</Preview>

# Props

<Props of={Button} />
