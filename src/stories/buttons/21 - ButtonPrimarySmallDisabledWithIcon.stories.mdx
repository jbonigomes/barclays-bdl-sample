import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import Button from '../../components/Button'

<Meta title="Button" />

# Preview

Primary small disabled button with icon

<Preview>
  <Story name="21 - Primary small disabled button with icon">
    <Button
      small
      disabled
      type="primary"
      icon="transfer"
      text="Button with icon"
      onClick={action('clicked')}
    />
  </Story>
</Preview>

# Props

<Props of={Button} />
