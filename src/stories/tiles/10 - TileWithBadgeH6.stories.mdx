import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import logo from '../../images/logo.png'
import promoImage from '../../images/promo_image.png'
import TileWithBadge from '../../components/TileWithBadge'

<Meta title="Tiles" />

# Preview

This tile uses a custom header level (h6).

<Preview>
  <Story name="10 - Tile with badge h6">
    <TileWithBadge
      badge={logo}
      headerLevel={6}
      image={promoImage}
      title="Cashback"
      onTitleClick={action('clicked')}
    >
      Get cashback when shopping at participant retailers
    </TileWithBadge>
  </Story>
</Preview>

# Props

<Props of={TileWithBadge} />
