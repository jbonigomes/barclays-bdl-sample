import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import Button from '../../components/Button'

<Meta title="Button" />

# Preview

Primary small buttons have a min width.

<Preview>
  <Story name="12 - Primary small button min width">
    <Button
      small
      text="Yes"
      type="primary"
      onClick={action('clicked')}
    />
  </Story>
</Preview>

# Props

<Props of={Button} />
