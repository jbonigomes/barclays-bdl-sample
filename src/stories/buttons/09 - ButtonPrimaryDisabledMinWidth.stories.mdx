import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import Button from '../../components/Button'

<Meta title="Button" />

# Preview

Disabled primary buttons have a min width.

<Preview>
  <Story name="09 - Disabled primary button min width">
    <Button
      disabled
      text="Yes"
      type="primary"
      onClick={action('clicked')}
    />
  </Story>
</Preview>

# Props

<Props of={Button} />
